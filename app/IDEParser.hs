module IDEParser where

--If we had some parsing, this is where it would go
--Current output is String, this needs to be altered to whatever parsed type we end up with
parseInput :: String -> String
parseInput = id